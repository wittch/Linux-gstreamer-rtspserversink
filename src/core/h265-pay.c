/* Intentionally empty.
 *
 * H.264/H.265 payload handling moved to the generic RTP relay path in
 * h264-pay.c. This translation unit is kept so the existing Meson source list
 * does not need to change in the same refactor.
 */
